# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/
.env

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Generated data — uncomment if regenerating locally and you don't want to
# track the snapshot. Default policy is: track the seed-42 snapshot so the
# repo is "clone-and-run" without needing to regenerate. Override per-fork.
# stage2_world/data/*.parquet

# Stage 3 prediction outputs from arbitrary dates (only the canonical
# 2024-12-15 sample is committed)
stage3_models/output/snapshot_*
!stage3_models/output/snapshot_2024-12-15/

# Local environment overrides
.env.local
*.local

# OS junk
Thumbs.db
desktop.ini
